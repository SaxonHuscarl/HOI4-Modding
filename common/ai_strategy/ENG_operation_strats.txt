ENG_boost_french_resistance = {
	allowed = {
		tag = ENG
	}
	
	enable = {
		check_variable = { generic_operation_target = GER }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_mission
		mission = build_intel_network
		value = 1000
		mission_target = GER
		state = var:selected_resistance_area@GER
	}
}
SOV_build_intel_network_near_front = {
	allowed = {
		tag = SOV
	}
	
	enable = {
		check_variable = { generic_operation_target = GER }
		has_variable = selected_resistance_area@GER
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_mission
		mission = build_intel_network
		value = 2000
		mission_target = GER
		state = var:selected_resistance_area@GER
		priority = 100
	}
}
GER_target_soviet_oil = {
	allowed = {
		tag = GER
	}
	
	enable = {
		check_variable = { generic_operation_target = SOV }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_mission
		mission = build_intel_network
		value = 2000
		mission_target = SOV
		state = 229 # Baku
	}
}
ENG_heavy_water_raids = {
	allowed = {
		tag = ENG
	}
	
	enable = {
		has_war_with = GER
		num_of_operatives > 4
		GER = {
			controls_state = 110
		}
		110 = {
			resistance > 52
		}
	}
	abort_when_not_enabled = yes
	
	
	ai_strategy = {
		type = operative_operation
		operation = ENG_heavy_water_raid
		value = 2000
		operation_target = GER
	}
	
	ai_strategy = {
		type = operative_mission
		mission = build_intel_network
		value = 1200
		mission_target = GER
		state = 110
	}
	
	ai_strategy = {
		type = operation_equipment_priority
		value = 100
	}
}

